      <!-- Insert the common header across the site -->

      {{> header}}

<!-- Main title for the club page, styled with a specific text color -->
<h1 class="text-[#dbebc0]">Club Page</h1>

<!-- Display the name of the club, using the same text color styling -->
<h1 class="text-[#dbebc0]">{{club.name}}</h1>

<!-- Check the user's role and display different content based on that role -->
{{!-- Different views based on user role --}}
{{#if userRole.isAdmin}}
    {{!-- Admin view --}}
    {{> adminDashboard}}
{{else if userRole.isMember}}
    {{!-- Member view --}}
    {{> memberView}}
{{else}}
    {{!-- Non-member view --}}
    {{> nonMemberView}}
{{/if}}

